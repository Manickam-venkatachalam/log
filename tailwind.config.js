const { colors } = require("@mui/material");

module.exports = {
  content: [],
  theme: {
    extend: {
      colors: {
        'cyan': colors.cyan,
        'teal': colors.teal
      }
    },
  },
  plugins: [],
}
